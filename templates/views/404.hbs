<!DOCTYPE html>

<html>
    <head>
        <title>404</title>
        {{>head}}
    </head>

    <body>
        <div class="main-content">
            {{>header}}
            <p>{{message}}</p>
        </div>
        
        {{>footer}}
    </body>
</html>